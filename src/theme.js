const themeColor = `#ff4100`
const textNormal = `rgb(217, 217, 217)`
const black = `#000`

const Theme = {
  backgroundColor: black,
  themeColor,
  textNormal,
  activeLink: themeColor,
  link: textNormal,
}

export default Theme